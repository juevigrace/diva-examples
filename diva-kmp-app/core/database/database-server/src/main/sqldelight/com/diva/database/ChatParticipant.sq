insert:
INSERT INTO diva_chat_participant (chat_id, user_id, role, added_by)
VALUES (?, ?, ?, ?);

update:
UPDATE diva_chat_participant 
SET role = ?, last_read_at = ?
WHERE chat_id = ? AND user_id = ?;

delete:
DELETE FROM diva_chat_participant 
WHERE chat_id = ? AND user_id = ?;