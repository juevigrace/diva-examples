insert:
INSERT INTO diva_message_media (message_id, media_id, position)
VALUES (?, ?, ?);

update:
UPDATE diva_message_media 
SET position = ? 
WHERE message_id = ? AND media_id = ?;

delete:
DELETE FROM diva_message_media 
WHERE message_id = ? AND media_id = ?;